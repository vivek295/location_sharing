<% provide(:title, 'Sign in') %>

<div class="container form-container">
    <div class="offset-md-3 col-md-6">
      <div class="card">
        <div class="card-header">Login</div>
        <div class="card-body">
          <%= form_for(resource, as: resource_name, url: session_path(resource_name),html: {class: 'form-horizontal', role: 'form'}) do |f| %>
            <div class="form-group">
              <%= f.label :email, class: "col-sm-3 control-label" %>
              <div class="col-sm-12">
                <%= f.email_field :email, class: "form-control" %>
              </div>
            </div>
            <div class="form-group">
              <%= f.label :password, class: "col-sm-3 control-label" %>
              <div class="col-sm-12">
                <%= f.password_field :password, class: "form-control" %>
              </div>
            </div>
            <div class="form-group last">
              <div class="col-sm-12">
                <%= f.submit "Log in", class: "btn btn-success btn-sm" %>
              </div>
            </div>
          <% end %>
        </div>
        <div class="card-footer">
          <div>Not Registred? <%= link_to "Register here", new_registration_path(resource_name) %><br /></div>
          <div>Forgot password? <%= link_to "Reset your password", new_password_path(resource_name) %><br /></div>
        </div>
      </div>
    </div>
</div>
